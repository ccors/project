# Make sure devtools is installed and loaded
install.packages("devtools")
library(devtools)

# Make sure slidify is installed and loaded
install_github('slidify', 'ramnathv')
install_github('slidifyLibraries', 'ramnathv')
library(slidify)

author("slidify")